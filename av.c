#include <stdio.h>
/**
 * main - prints all args without ac
 * @ac: numberof arg
 * @av: array of strings
 *
 * Return: 0 if success
 */

int main(int ac, char **av)
{
	int it;
	for (it = 0; it < ac; it++)
	{
		(void) ac;
		printf("%s\n", av[i]);
	}
	return (0);
}
